---
apiVersion: gateway.networking.k8s.io/v1
kind: GRPCRoute
metadata:
  name: color-route
  namespace: faces
spec:
  parentRefs:
    - group: ""
      kind: Service
      name: color
      namespace: faces
      port: 80
  rules:
    - backendRefs:
        - group: ""
          kind: Service
          name: color
          namespace: faces
          port: 80
